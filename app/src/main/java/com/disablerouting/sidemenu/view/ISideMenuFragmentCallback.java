package com.disablerouting.sidemenu.view;

public interface ISideMenuFragmentCallback {

    /**
     * on click of side menu view then close drawer
     * @param close
     */
    void onClick(int close);
}
